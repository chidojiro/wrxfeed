import * as React from 'react';

/**
 * Buttons container
 */
// const ActionButtons = styled('div')(({ theme }) => ({
//   display: 'flex',
//   justifyContent: 'center',
//   gridColumnGap: theme.spacing(2),
// }));

const ActionButtons = () => <div className="flex justify-center" />;

export default ActionButtons;
